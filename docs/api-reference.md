# 🌐 API Reference (v8.2)

คู่มือการส่งข้อมูลเพื่อ Encode และ Decode ผ่าน GhostPass API (ฉบับปรับปรุง Algorithm ใหม่)

## 🏠 Endpoint พื้นฐาน
เมือคุณ Deploy บน Vercel หรือ Server อื่นๆ URL พื้นฐานจะเป็น:
`https://key-genesis.vercel.app/api` (Production)

---

## 🔒 ฟีเจอร์ใหม่: Scrambling Layer (v8.2)
ในเวอร์ชันนี้เราได้เพิ่มระบบ **Position-based Salt** เพื่อให้มั่นใจว่า:
- **High Entropy**: แม้ Metadata จะมีค่าซ้ำกัน (เช่น เลข 0 ทั้งหมด) แต่คำ 12 คำที่ได้จะกระจายตัวและไม่ซ้ำกัน
- **Security**: รหัสที่ได้จะดูเป็นมืออาชีพเหมือน Crypto Wallet ทั่วไป
- **Deterministic**: ข้อมูลเดิม + Secret เดิม จะได้คำเดิมเสมอ และถอดรหัสกลับมาได้ 100%

---

## 🔑 1. การสร้างกุญแจ (Encode API)
ใช้เพื่อรับข้อมูล Licensing แล้วแปลงเป็น 12 คำศัพท์

- **Method:** `POST`
- **URL:** `/api/encode`
- **Body (JSON):**
```json
{
  "role": 1,
  "type": 0,
  "name": 500,
  "sku": 777
}
```

- **Response ตัวอย่าง:**
```json
{
  "success": true,
  "phrase": ["document", "napkin", "excess", ...],
  "words": "document napkin excess ..."
}
```

---

## 🔍 2. การตรวจสอบกุญแจ (Decode API)
ใช้เพื่อรับ 12 คำศัพท์ แล้วถอดรหัสออกมาเป็นข้อมูล พร้อมเช็คความถูกต้อง (รปภ)

- **Method:** `POST`
- **URL:** `/api/decode`
- **Body (JSON):**
```json
{
  "phrase": "abandon ability able ..."
}
```
*(หรือจะส่งเป็น Array ของคำศัพท์ก็ได้)*

- **Response ตัวอย่าง:**
```json
{
  "success": true,
  "valid": true,
  "data": {
    "role": 1,
    "type": 0,
    "name": 500,
    "version": 1,
    ...
  }
}
```

---

## 💻 ตัวอย่างการเรียกใช้ (JavaScript Fetch)
```javascript
const response = await fetch('https://your-api.com/api/decode', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ phrase: "word1 word2 ... word12" })
});

const result = await response.json();
if (result.valid) {
  console.log("เข้าใช้งานได้! บทบาทคือ:", result.data.role);
} else {
  console.log("กุญแจไม่ถูกต้อง!");
}
```
