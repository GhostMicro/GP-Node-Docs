# GhostPass v8.1 User Manual

คู่มือการใช้งานระบบกุญแจ 12 คำ (GhostPass) สำหรับ GhostMicro

## 🛡️ ระบบความปลอดภัย Master Authority
GhostPass v8.1 ใช้ระบบ **Secret Salt** เพื่อให้กุญแจ 12 คำของคุณมีความเฉพาะตัวสูงสุด โดย "ผู้คุ้มกฎ" (คำที่ 12) จะถูกผูกไว้กับรหัสลับที่คุณตั้งเอง

1. **การตั้งรหัสรหัสลับ:** ที่ช่อง **Master Authority** บน Dashboard ให้กรอกรหัสลับส่วนตัวของคุณ
2. **การทำงาน:** คำที่ 12 จะเปลี่ยนไปตามรหัสนั้นทันที
3. **การตรวจสอบ:** ต้องใช้รหัสเดียวกันในการสร้างและตรวจสอบ "ผู้คุ้มกฎ" ถึงจะอนุญาตให้ผ่านได้

---

## 🔑 กฎ 12 หลัก (The 12-Word Rule)
ระบบ GhostPass แจกแจงข้อมูลใบอนุญาตออกเป็นคำศัพท์ 12 คำ โดยแต่ละตำแหน่ง (Position) มีความหมายเฉพาะตัวดังนี้:

| ตำแหน่ง | ฟิลด์ข้อมูล (Field) | ตัวอย่างค่าที่ใช้ได้ |
| :--- | :--- | :--- |
| **1** | **Role (บทบาท)** | 0=Dev, 1=Admin, 2=User, 3=Member |
| **2** | **Type (ประเภท)** | 0=Software, 1=Hardware, 2=App |
| **3** | **Name (ชื่อ)** | รหัสประจำชื่อสินค้า (0 - 2047) |
| **4** | **Reserved** | *(ว่าง / สำรองสำหรับอนาคต)* |
| **5** | **Version** | เวอร์ชันหลักของซอฟต์แวร์ |
| **6** | **Model** | รหัสรุ่นหรือรหัสเครื่อง |
| **7** | **Production Date** | วันที่ผลิต (รูปแบบ YYMMDD) |
| **8** | **Activation Date** | วันที่เริ่มเปิดใช้งาน |
| **9** | **Expiry Date** | วันหมดอายุ (2047 = ตลอดชีพ) |
| **10** | **SKU** | รหัสสินค้าในสต็อก |
| **11** | **Reserved** | *(ว่าง / สำรองสำหรับอนาคต)* |
| **12** | **Security (ผู้คุ้มกฎ)** | **จุดตรวจสอบความถูกต้อง (Checksum)** |

---

## 🌐 การใช้งานผ่าน API (Headless Node)
ในเวอร์ชัน 8.1 เป็นต้นไป ระบบ GhostPass จะทำงานเป็น **Headless Node** (ไม่มีหน้าจอจัดการคีย์โดยตรง) เพื่อความปลอดภัยสูงสุด การสร้างและตรวจสอบคีย์จะทำผ่าน API เท่านั้น

### ขั้นตอนการสร้างกุญแจ
1. เข้าไปอ่านคู่มือที่เมนู **🌐 API Reference**
2. ส่งข้อมูลที่ต้องการ (Role, Type, Model...) ไปยังตำแหน่งปลายทาง `/api/encode`
3. ระบบจะตอบกลับเป็นคำศัพท์ 12 คำทันที

### ขั้นตอนการตรวจสอบกุญแจ
1. ส่งคำศัพท์ 12 คำไปยังตำแหน่งปลายทาง `/api/decode`
2. ระบบจะทำการตรวจสอบ "ผู้คุ้มกฎ" (Checksum) และคืนค่าความถูกต้องกลับมา

---

## 🛡 ข้อควรระวัง
- **Master Authority สำคัญที่สุด**: หากรหัสลับที่ตั้งไว้ในระบบเปลี่ยนไป คำที่ 12 จะเปลี่ยนตามทันที
- **ลำดับคำสำคัญที่สุด**: หากสลับตำแหน่งคำแม้แต่คู่เดียว ข้อมูลภายในจะเปลี่ยนไปทันที
- **ห้ามสะกดผิด**: ระบบใช้ฐานข้อมูลคำศัพท์ 2,048 คำมาตรฐาน (BIP-39) การสะกดผิดจะทำให้ระบบไม่รู้จักคำนั้นๆ

---

## 🛡 ข้อควรระวัง
- **ลำดับคำสำคัญที่สุด**: หากสลับตำแหน่งคำแม้แต่คู่เดียว ข้อมูลภายในจะเปลี่ยนไปทันที
- **ห้ามสะกดผิด**: ระบบใช้ฐานข้อมูลคำศัพท์ 2,048 คำมาตรฐาน (BIP-39) การสะกดผิดจะทำให้ระบบไม่รู้จักคำนั้นๆ
