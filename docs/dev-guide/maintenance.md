# คู่มือการบำรุงรักษา (Maintenance Guide)

เอกสารสำหรับนักพัฒนาเพื่อแก้ไขหรือเพิ่มรายการข้อมูลในระบบ GhostPass v8.1

## 📋 การแก้ไขรายการ Dropdown (Roles, Types, Products)

หากต้องการเพิ่มสินค้าใหม่ หรือเปลี่ยนชื่อบทบาท ให้แก้ไขที่ไฟล์ Dashboard หลัก:

**ไฟล์ที่ต้องแก้ไข:** `c:/Users/User/Documents/GitHub/GhostMicro/GhostPassV8/app/page.tsx`

### 1. การเพิ่มสินค้าใหม่ (Products)
ค้นหาบรรทัดที่มีการนิยามตัวแปร `PRODUCTS` และเพิ่มรายการต่อท้าย:

```tsx
const PRODUCTS = [
  'IoT Smart Home', // Index 0
  'IoT Smart Farm', // Index 1
  'Robot',          // Index 2
  'Smart City',     // New Index 3
];
```

> [!CAUTION]
> **ลำดับ (Index) สำคัญมาก!**
> ห้ามสลับลำดับรายการเดิมเด็ดขาด เพราะเลขลำดับจะถูกนำไปใช้ Gen กุญแจ 12 คำ หากลำดับเปลี่ยน กุญแจเดิมที่เคยแจกจ่ายไปแล้วจะตรวจสอบไม่ผ่าน (Invalid) หรือข้อมูลเพี้ยนทันที

### 2. การแก้ไขบทบาท (Roles) หรือ ประเภท (Types)
แก้ไขที่ตัวแปร `ROLES` และ `TYPES` ในไฟล์เดียวกัน:

```tsx
const ROLES = ['Developer', 'Administrator', 'User', 'Member'];
const TYPES = ['Software', 'Hardware', 'App'];
```

---

## 🛠 การอัปเดตระบบตรวจสอบ (Verification Logic)

ระบบตรวจสอบความถูกต้องใช้ Common Logic ที่แชร์กันระหว่าง Repository:
- `lib/mnemonic.ts` (ใน GhostPassV8 และ key-genesis)

หากมีการเปลี่ยนโครงสร้างข้อมูล (เช่น เพิ่ม Word ที่ 13) จะต้องแก้ไขฟังก์ชัน `encodeGhostPass` และ `decodeGhostPass` ให้ตรงกันทั้งสองที่

---

## 📄 การอัปเดตคู่มือ (Documentation)
เมื่อมีการเพิ่มรายการใหม่ อย่าลืมอัปเดตตารางใน `user-guide.md` ใน Repository `GP-Node-Docs` เพื่อให้ผู้ใช้ทราบค่า Index ที่ถูกต้องสำหรับการใช้งานผ่าน API

---
*บันทึกข้อมูลล่าสุด: 2026-01-03*
